{
  "context": "Evaluates correct use of spprof.is_active() and spprof.stats() for monitoring profiler state. Checks that the candidate uses is_active() to check status at multiple points and calls stats() to retrieve live statistics, handling the None return when profiling is inactive.",
  "type": "weighted_checklist",
  "capability_tested": "Profiler Status and Statistics",
  "checklist": [
    {
      "name": "is_active() before start",
      "description": "spprof.is_active() is called before start() and shown to return False",
      "max_score": 20
    },
    {
      "name": "is_active() after start",
      "description": "spprof.is_active() is called after start() and shown to return True",
      "max_score": 20
    },
    {
      "name": "stats() called while active",
      "description": "spprof.stats() is called while profiling is active and collected_samples is accessed from the result",
      "max_score": 25
    },
    {
      "name": "is_active() after stop",
      "description": "spprof.is_active() is called after stop() and shown to return False",
      "max_score": 20
    },
    {
      "name": "stats() returns None after stop",
      "description": "spprof.stats() is called after stop() and the None return is handled and printed",
      "max_score": 15
    }
  ]
}
